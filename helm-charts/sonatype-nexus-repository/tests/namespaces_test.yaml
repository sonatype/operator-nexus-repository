suite: test namespaces
templates:
  - namespaces.yaml
values:
  - test-values.yaml
tests:
  - it: should create namespace
    asserts:
      - isKind:
          of: Namespace
      - equal:
          path: metadata.name
          value: "test-nexusrepo"